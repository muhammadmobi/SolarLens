# Copy to .dev.vars (gitignored) and fill in. Never commit real values.

# --- SolisCloud: Service -> API Management -> Activate Now ---
SOLIS_KEY_ID=
SOLIS_KEY_SECRET=

# --- SolisCloud web-session fallback (used only when the keys above are absent) ---
# Log in at https://www.soliscloud.com, DevTools -> Network -> any /api/ request ->
# Request Headers: copy the auth header's value here and its name below. Unofficial; may break.
SOLIS_WEB_TOKEN=
SOLIS_WEB_TOKEN_HEADER=token

# --- SolarMan official Business API: appId/appSecret from service@solarmanpv.com ---
SOLARMAN_APP_ID=
SOLARMAN_APP_SECRET=
SOLARMAN_EMAIL=
# sha256 hex of your SolarMan password (lowercase, no salt)
SOLARMAN_PASSWORD_SHA256=

# --- SolarMan web-session fallback (used only when the official keys above are absent) ---
# Log in to https://home.solarmanpv.com in a browser, then DevTools -> Application ->
# Local Storage -> home.solarmanpv.com and copy the refresh token. Unofficial; may break.
SOLARMAN_WEB_REFRESH_TOKEN=
SOLARMAN_WEB_ACCESS_TOKEN=

# --- Which plants to poll (vendor ids, comma-separated). Unset = all visible plants. ---
# Solis "Solis Ongrid" = <your-solis-plant-id>, SolarMan "Nitrox Hybrid" = <your-solarman-station-id>
INCLUDE_PLANTS=<your-solis-plant-id>,<your-solarman-station-id>

# --- Shared secret gating /api/* and the local agent's /api/ingest ---
API_TOKEN=
INGEST_TOKEN=
